# Asynchronous-FIFO
An Asynchronous FIFO is a dual-clock buffer used to transfer data between different clock domains. It prevents data loss and metastability by using Gray code pointers and 2-stage synchronizers. Full/Empty flags are generated by comparing the write and read pointers after they safely cross into the opposite clock domain.
